apiVersion: v1
kind: Service
metadata:
  name: wishlist-api-service
  labels:
    app: wishlist-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: wishlist-api
